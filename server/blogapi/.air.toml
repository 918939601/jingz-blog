    root = "."
    tmp_dir = "tmp"

    [build]
    cmd = "go build -o ./tmp/blogapi ./blogapi.go"
    bin = "tmp/blogapi"
    full_bin = "tmp/blogapi -f etc/blogapi.yaml"
    include_ext = ["go","yaml"]
    exclude_dir = ["tmp","vendor"]
    delay = 1000